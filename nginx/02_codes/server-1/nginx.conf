events {}

http {

    # Load MIME types (so CSS, JS, images, etc. are recognized properly)
    include       mime.types;

    # Fallback type if extension is unknown
    default_type  application/octet-stream;

    server {
        listen 8080;
        root /mnt/d/My_Workings/SkillStack/nginx/02_codes/server-1;
        index 01_index.html;

        location / {
            try_files $uri $uri/ =404;
        }

        location ~* /count/[0-9] {
            root /mnt/d/My_Workings/SkillStack/nginx/02_codes/server-1;
            try_files /01_index.html = 404;
        }

        location /fruits {
           root /mnt/d/My_Workings/SkillStack/nginx/02_codes/server-1;
           autoindex on;
        }

        location /carbs {
            alias /mnt/d/My_Workings/SkillStack/nginx/02_codes/server-1;
        }

        location /vegetables {
            root /mnt/d/My_Workings/SkillStack/nginx/02_codes/server-1;
            try_files /vegetables/veggies.html /01_index.html = 404;
        }

        location /corps {
            return 307 /fruits;
        }

        rewrite ^/number/(\w+) /count/$1;
    }
}

# write all this configuration file things in the original nginx.conf file on the wsl using vi